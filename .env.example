# API Configuration
VITE_API_URL=https://api.example.com    # Your API endpoint (use http://localhost:3001 for local development)
VITE_API_KEY=your_api_key               # Your API authentication key
VITE_API_PROXY=/api                     # API proxy path used in vite.config.js

# Application Configuration
VITE_APP_TITLE=SignForMe AI            # Application title
VITE_BASE_URL=http://localhost:4000    # Frontend application URL

# Environment Configuration
NODE_ENV=development                   # Environment mode (development/production)

# Server Configuration
PORT=4000                             # Vite development server port
API_PORT=3001                         # JSON server port

# This is an example environment file
# 1. Copy this file to .env and adjust values as needed
# 2. Note: This project uses Vite, so environment variables must be prefixed with VITE_
#    (Don't use REACT_APP_ prefix as it's specific to Create React App)
# 3. Only variables prefixed with VITE_ will be exposed to your application
# 4. In production, ensure you set proper values for VITE_API_URL and VITE_API_KEY
